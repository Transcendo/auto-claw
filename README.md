# auto-code monorepo

This repo contains a modern TypeScript CLI template built with Vite.

## Packages

- `packages/cli`: CLI package (binary name: `auto-code`)

## Quick start

- Install deps: `pnpm install`
- Build all: `pnpm build`
- Start CLI: `pnpm start:cli -- hello`
